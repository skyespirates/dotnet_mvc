@if(TempData["created"] != null) {
  <div class="alert alert-primary">@TempData["created"]</div>
}
@if(TempData["updated"] != null) {
  <div class="alert alert-warning">@TempData["updated"]</div>
}
@if(TempData["deleted"] != null) {
  <div class="alert alert-danger">@TempData["deleted"]</div>
}